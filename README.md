# Rhapsody TV

This repository contains two separate applications:

## 📱 Mobile App (`mobile-app/`)

React Native mobile application built with Expo.

- Navigate to `mobile-app/` directory
- Run `npm install` to install dependencies
- Run `npm start` to start the development server

## 🖥️ Admin App (`admin-app/`)

React web application for admin management, built with Vite.

- Navigate to `admin-app/` directory
- Run `npm install` to install dependencies
- Run `npm run dev` to start the development server

## Structure

```
Rhapsody_tv/
├── mobile-app/     # Mobile application (React Native/Expo)
└── admin-app/      # Admin web application (React/Vite)
```

Each application has its own dependencies, build configuration, and can be built independently.
